diff --git a/src/llama.rn/vendor/llama.cpp/common/CMakeLists.txt b/src/llama.rn/vendor/llama.cpp/common/CMakeLists.txt
index 38dab96a..79c78507 100644
--- a/src/llama.rn/vendor/llama.cpp/common/CMakeLists.txt
+++ b/src/llama.rn/vendor/llama.cpp/common/CMakeLists.txt
@@ -185,4 +185,10 @@ if (LLAMA_LLGUIDANCE)
     endif()
 endif()
 
-target_link_libraries(${TARGET} PUBLIC llama Threads::Threads)
+if (WIN32)
+    set(LLAMA_COMMON_WIN_LIBS ws2_32)
+else()
+    set(LLAMA_COMMON_WIN_LIBS "")
+endif()
+
+target_link_libraries(${TARGET} PRIVATE ${LLAMA_COMMON_WIN_LIBS} PUBLIC llama Threads::Threads)
diff --git a/src/llama.rn/vendor/llama.cpp/common/chat-diff-analyzer.cpp b/src/llama.rn/vendor/llama.cpp/common/chat-diff-analyzer.cpp
index a7e37057..ac919abc 100644
--- a/src/llama.rn/vendor/llama.cpp/common/chat-diff-analyzer.cpp
+++ b/src/llama.rn/vendor/llama.cpp/common/chat-diff-analyzer.cpp
@@ -438,7 +438,7 @@ std::string autoparser::detect_user_start_marker(const common_chat_template & tm
 
     auto candidate = usermsg.substr(0, usermsg.find(USER_MSG));
     auto candidate_split = segmentize_markers(candidate);
-    std::stringstream result;
+    std::string result;
     bool encountered_marker = false;
     for (const auto & mrk : candidate_split) {
         std::string lower_mrk = std::string(mrk.value);
@@ -453,9 +453,9 @@ std::string autoparser::detect_user_start_marker(const common_chat_template & tm
             continue;
         }
         encountered_marker |= mrk.type == segment_type::MARKER;
-        result << mrk.value;
+        result += mrk.value;
     }
-    return trim_whitespace(result.str());
+    return trim_whitespace(result);
 }
 
 analyze_reasoning::analyze_reasoning(const common_chat_template & tmpl, bool supports_tools)
diff --git a/src/llama.rn/vendor/llama.cpp/common/chat.cpp b/src/llama.rn/vendor/llama.cpp/common/chat.cpp
index b3455dc1..c0ad7d26 100644
--- a/src/llama.rn/vendor/llama.cpp/common/chat.cpp
+++ b/src/llama.rn/vendor/llama.cpp/common/chat.cpp
@@ -35,11 +35,22 @@ using json = common_json;
 
 static std::string format_time(const std::chrono::system_clock::time_point & now, const std::string & format) {
     auto               time       = std::chrono::system_clock::to_time_t(now);
-    auto               local_time = *std::localtime(&time);
-    std::ostringstream ss;
-    ss << std::put_time(&local_time, format.c_str());
-    auto res = ss.str();
-    return res;
+    std::tm            local_time = {};
+#if defined(_WIN32)
+    if (localtime_s(&local_time, &time) != 0) {
+        return "";
+    }
+#else
+    if (localtime_r(&time, &local_time) == nullptr) {
+        return "";
+    }
+#endif
+
+    char buf[64];
+    if (std::strftime(buf, sizeof(buf), format.c_str(), &local_time) == 0) {
+        return "";
+    }
+    return buf;
 }
 
 static json safe_args_parse(const std::string & to_parse) {
diff --git a/src/llama.rn/vendor/llama.cpp/common/jinja/string.cpp b/src/llama.rn/vendor/llama.cpp/common/jinja/string.cpp
index bde679e4..209efefb 100644
--- a/src/llama.rn/vendor/llama.cpp/common/jinja/string.cpp
+++ b/src/llama.rn/vendor/llama.cpp/common/jinja/string.cpp
@@ -46,11 +46,13 @@ std::string string::str() const {
     if (parts.size() == 1) {
         return parts[0].val;
     }
-    std::ostringstream oss;
+
+    std::string out;
+    out.reserve(length());
     for (const auto & part : parts) {
-        oss << part.val;
+        out += part.val;
     }
-    return oss.str();
+    return out;
 }
 
 size_t string::length() const {
diff --git a/src/llama.rn/vendor/llama.cpp/common/log.cpp b/src/llama.rn/vendor/llama.cpp/common/log.cpp
index dc8cbbe3..7d86cf29 100644
--- a/src/llama.rn/vendor/llama.cpp/common/log.cpp
+++ b/src/llama.rn/vendor/llama.cpp/common/log.cpp
@@ -18,6 +18,12 @@
 #include <android/log.h>
 #endif
 
+#if defined(__EMSCRIPTEN__) && !defined(__EMSCRIPTEN_PTHREADS__)
+#define COMMON_LOG_SYNC 1
+#else
+#define COMMON_LOG_SYNC 0
+#endif
+
 #if defined(_WIN32)
 #    define WIN32_LEAN_AND_MEAN
 #    ifndef NOMINMAX
@@ -214,7 +220,11 @@ struct common_log {
         head = 0;
         tail = 0;
 
+#if COMMON_LOG_SYNC
+        running = true;
+#else
         resume();
+#endif
     }
 
     ~common_log() {
@@ -284,6 +294,37 @@ public:
             return;
         }
 
+#if COMMON_LOG_SYNC
+        common_log_entry sync_entry;
+        sync_entry.msg.resize(256);
+        {
+            va_list args_copy;
+            va_copy(args_copy, args);
+
+            const size_t n = vsnprintf(sync_entry.msg.data(), sync_entry.msg.size(), fmt, args);
+            if (n >= sync_entry.msg.size()) {
+                sync_entry.msg.resize(n + 1);
+                vsnprintf(sync_entry.msg.data(), sync_entry.msg.size(), fmt, args_copy);
+            }
+
+            va_end(args_copy);
+        }
+
+        sync_entry.level = level;
+        sync_entry.prefix = prefix;
+        sync_entry.timestamp = 0;
+        if (timestamps) {
+            sync_entry.timestamp = t_us() - t_start;
+        }
+        sync_entry.is_end = false;
+
+        sync_entry.print();
+        if (file) {
+            sync_entry.print(file);
+        }
+        return;
+#endif
+
         auto & entry = queue[tail];
 
         {
@@ -378,6 +419,9 @@ public:
 
         running = true;
 
+#if COMMON_LOG_SYNC
+        return;
+#else
         thrd = std::thread([this]() {
             while (true) {
                 std::unique_lock<std::mutex> lock(mtx);
@@ -400,6 +444,7 @@ public:
                 }
             }
         });
+#endif
     }
 
     void pause() {
@@ -412,6 +457,10 @@ public:
 
             running = false;
 
+#if COMMON_LOG_SYNC
+            return;
+#endif
+
             // push an entry to signal the worker thread to stop
             auto & entry = queue[tail];
             entry.is_end = true;
diff --git a/src/llama.rn/vendor/llama.cpp/common/parsers/sources.cmake b/src/llama.rn/vendor/llama.cpp/common/parsers/sources.cmake
index 9d7fb099..5add69fb 100644
--- a/src/llama.rn/vendor/llama.cpp/common/parsers/sources.cmake
+++ b/src/llama.rn/vendor/llama.cpp/common/parsers/sources.cmake
@@ -5,6 +5,7 @@ set(LLAMA_CHAT_PARSERS_SOURCES
     ${CMAKE_CURRENT_LIST_DIR}/parsers.h
     ${CMAKE_CURRENT_LIST_DIR}/cohere2moe.cpp
     ${CMAKE_CURRENT_LIST_DIR}/deepseek.cpp
+    ${CMAKE_CURRENT_LIST_DIR}/function-gemma.cpp
     ${CMAKE_CURRENT_LIST_DIR}/functionary-v3-2.cpp
     ${CMAKE_CURRENT_LIST_DIR}/gemma4.cpp
     ${CMAKE_CURRENT_LIST_DIR}/gigachat-v3.cpp
diff --git a/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-cpu/CMakeLists.txt b/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-cpu/CMakeLists.txt
index 83088e14..d31e47f8 100644
--- a/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-cpu/CMakeLists.txt
+++ b/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-cpu/CMakeLists.txt
@@ -105,7 +105,7 @@ function(ggml_add_cpu_backend_variant_impl tag_name)
             )
 
         if (MSVC AND NOT CMAKE_C_COMPILER_ID STREQUAL "Clang")
-            message(FATAL_ERROR "MSVC is not supported for ARM, use clang")
+            list(APPEND ARCH_FLAGS /arch:armv8.7)
         else()
             check_cxx_compiler_flag(-mfp16-format=ieee GGML_COMPILER_SUPPORTS_FP16_FORMAT_I3E)
             if (NOT "${GGML_COMPILER_SUPPORTS_FP16_FORMAT_I3E}" STREQUAL "")
diff --git a/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-hexagon/ggml-hexagon.cpp b/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-hexagon/ggml-hexagon.cpp
index 97d99a15..2fb574bd 100644
--- a/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-hexagon/ggml-hexagon.cpp
+++ b/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-hexagon/ggml-hexagon.cpp
@@ -6090,8 +6090,24 @@ static const char * ggml_backend_hexagon_device_get_description(ggml_backend_dev
     GGML_UNUSED(dev);
 }
 
+// ~2GB per session for now
+#define GGML_HEXAGON_SESSION_MEMORY_DEFAULT (2ULL * 1024 * 1024 * 1024)
+// Max to 3.5GB
+#define GGML_HEXAGON_SESSION_MEMORY_MAX (3ULL * 1024 * 1024 * 1024 + 512ULL * 1024 * 1024)
+
 static void ggml_backend_hexagon_device_get_memory(ggml_backend_dev_t dev, size_t * free, size_t * total) {
-    *free  = 0;
+    const char * str_mem = getenv("GGML_HEXAGON_SESSION_MEMORY");
+    if (str_mem) {
+        *free = std::stoull(str_mem);
+        if (*free < GGML_HEXAGON_SESSION_MEMORY_DEFAULT) {
+            *free = GGML_HEXAGON_SESSION_MEMORY_DEFAULT;
+        } else if (*free > GGML_HEXAGON_SESSION_MEMORY_MAX) {
+            *free = GGML_HEXAGON_SESSION_MEMORY_MAX;
+        }
+    } else {
+        *free = GGML_HEXAGON_SESSION_MEMORY_DEFAULT;
+    }
+
     *total = *free;
 
     GGML_UNUSED(dev);
@@ -7039,7 +7055,12 @@ static void ggml_hexagon_init(ggml_backend_reg * reg) {
 #if defined(__ANDROID__)
     if (opt_arch < 75) {
         opt_ndev = 1;
-        GGML_LOG_WARN("ggml-hex: forcing ndev to 1 for SoCs archs lower than v75.\n");
+        GGML_LOG_WARN("ggml-hex: forcing ndev to 1 for SoCs archs lower than v75 for Android.\n");
+    }
+#else
+    if (opt_arch < 73) {
+        opt_ndev = 1;
+        GGML_LOG_WARN("ggml-hex: forcing ndev to 1 for SoCs archs lower than v73 for Linux and Windows.\n");
     }
 #endif
 
diff --git a/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-vulkan/CMakeLists.txt b/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-vulkan/CMakeLists.txt
index e733ad5c..2dd7d9bf 100644
--- a/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-vulkan/CMakeLists.txt
+++ b/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-vulkan/CMakeLists.txt
@@ -149,7 +149,7 @@ if (Vulkan_FOUND)
     endif()
 
     # Set up toolchain for host compilation whether cross-compiling or not
-    if (CMAKE_CROSSCOMPILING)
+    if (CMAKE_CROSSCOMPILING OR NOT CMAKE_HOST_SYSTEM_PROCESSOR STREQUAL CMAKE_SYSTEM_PROCESSOR)
         if (GGML_VULKAN_SHADERS_GEN_TOOLCHAIN)
             set(HOST_CMAKE_TOOLCHAIN_FILE ${GGML_VULKAN_SHADERS_GEN_TOOLCHAIN})
         else()
@@ -169,7 +169,7 @@ if (Vulkan_FOUND)
 
     include(ExternalProject)
 
-    if (CMAKE_CROSSCOMPILING)
+    if (CMAKE_CROSSCOMPILING OR NOT CMAKE_HOST_SYSTEM_PROCESSOR STREQUAL CMAKE_SYSTEM_PROCESSOR)
         list(APPEND VULKAN_SHADER_GEN_CMAKE_ARGS -DCMAKE_TOOLCHAIN_FILE=${HOST_CMAKE_TOOLCHAIN_FILE})
         message(STATUS "vulkan-shaders-gen toolchain file: ${HOST_CMAKE_TOOLCHAIN_FILE}")
     endif()
diff --git a/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-webgpu/pre_wgsl.hpp b/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-webgpu/pre_wgsl.hpp
index fb41a961..6587883c 100644
--- a/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-webgpu/pre_wgsl.hpp
+++ b/src/llama.rn/vendor/llama.cpp/ggml/src/ggml-webgpu/pre_wgsl.hpp
@@ -36,10 +36,19 @@ static std::string trim(const std::string & s) {
     return s.substr(a, b - a);
 }
 
-static std::string trim_value(std::istream & is) {
-    std::ostringstream ss;
-    ss << is.rdbuf();
-    return trim(ss.str());
+static std::string next_token(const std::string & s, size_t & pos) {
+    while (pos < s.size() && std::isspace((unsigned char) s[pos])) {
+        pos++;
+    }
+    const size_t start = pos;
+    while (pos < s.size() && !std::isspace((unsigned char) s[pos])) {
+        pos++;
+    }
+    return s.substr(start, pos - start);
+}
+
+static std::string trim_from(const std::string & s, size_t pos) {
+    return pos < s.size() ? trim(s.substr(pos)) : "";
 }
 
 static bool isIdentChar(char c) {
@@ -607,20 +616,34 @@ class Preprocessor {
                               const std::unordered_set<std::string> &        predefined_macros,
                               std::unordered_set<std::string> &              include_stack,
                               DirectiveMode                                  mode) {
-        std::vector<Cond>  cond;  // Conditional stack for this shader
-        std::stringstream  out;
-        std::istringstream in(shader_code);
-        std::string        line;
+        std::vector<Cond> cond;  // Conditional stack for this shader
+        std::string       out;
+        size_t            pos = 0;
+
+        while (pos < shader_code.size()) {
+            const size_t line_end = shader_code.find('\n', pos);
+            std::string  line     = shader_code.substr(
+                pos, line_end == std::string::npos ? std::string::npos : line_end - pos);
+            if (!line.empty() && line.back() == '\r') {
+                line.pop_back();
+            }
+            pos = line_end == std::string::npos ? shader_code.size() : line_end + 1;
 
-        while (std::getline(in, line)) {
             std::string logical = line;
             std::string t       = trim(logical);
             if (!t.empty() && t[0] == '#') {
                 while (endsWithContinuation(logical)) {
                     stripContinuation(logical);
-                    if (!std::getline(in, line)) {
+                    if (pos >= shader_code.size()) {
                         break;
                     }
+                    const size_t next_line_end = shader_code.find('\n', pos);
+                    line = shader_code.substr(
+                        pos, next_line_end == std::string::npos ? std::string::npos : next_line_end - pos);
+                    if (!line.empty() && line.back() == '\r') {
+                        line.pop_back();
+                    }
+                    pos = next_line_end == std::string::npos ? shader_code.size() : next_line_end + 1;
                     logical += "\n";
                     logical += line;
                 }
@@ -630,15 +653,18 @@ class Preprocessor {
             if (!t.empty() && t[0] == '#') {
                 bool handled = handleDirective(t, out, macros, predefined_macros, cond, include_stack, mode);
                 if (mode == DirectiveMode::IncludesOnly && !handled) {
-                    out << logical << "\n";
+                    out += logical;
+                    out += '\n';
                 }
             } else {
                 if (mode == DirectiveMode::IncludesOnly) {
-                    out << logical << "\n";
+                    out += logical;
+                    out += '\n';
                 } else if (condActive(cond)) {
                     // Expand macros in the line before outputting
                     std::string expanded = expandMacrosRecursive(logical, macros);
-                    out << expanded << "\n";
+                    out += expanded;
+                    out += '\n';
                 }
             }
         }
@@ -647,35 +673,33 @@ class Preprocessor {
             throw std::runtime_error("Unclosed #if directive");
         }
 
-        return out.str();
+        return out;
     }
 
     //----------------------------------------------------------
     // Directive handler
     //----------------------------------------------------------
     bool handleDirective(const std::string &                            t,
-                         std::stringstream &                            out,
+                         std::string &                                  out,
                          std::unordered_map<std::string, std::string> & macros,
                          const std::unordered_set<std::string> &        predefined_macros,
                          std::vector<Cond> &                            cond,
                          std::unordered_set<std::string> &              include_stack,
                          DirectiveMode                                  mode) {
         // split into tokens
-        std::string        body = t.substr(1);
-        std::istringstream iss(body);
-        std::string        cmd;
-        iss >> cmd;
+        std::string body = t.substr(1);
+        size_t      pos  = 0;
+        std::string cmd  = next_token(body, pos);
 
         if (cmd == "include") {
             if (mode == DirectiveMode::All && !condActive(cond)) {
                 return true;
             }
-            std::string file;
-            iss >> file;
+            std::string file = next_token(body, pos);
             if (file.size() >= 2 && file.front() == '"' && file.back() == '"') {
                 file = file.substr(1, file.size() - 2);
             }
-            out << processIncludeFile(file, macros, predefined_macros, include_stack, mode);
+            out += processIncludeFile(file, macros, predefined_macros, include_stack, mode);
             return true;
         }
 
@@ -687,13 +711,12 @@ class Preprocessor {
             if (!condActive(cond)) {
                 return true;
             }
-            std::string name;
-            iss >> name;
+            std::string name = next_token(body, pos);
             // Don't override predefined macros from options
             if (predefined_macros.count(name)) {
                 return true;
             }
-            std::string value = trim_value(iss);
+            std::string value = trim_from(body, pos);
             macros[name]      = value;
             return true;
         }
@@ -702,8 +725,7 @@ class Preprocessor {
             if (!condActive(cond)) {
                 return true;
             }
-            std::string name;
-            iss >> name;
+            std::string name = next_token(body, pos);
             // Don't undef predefined macros from options
             if (predefined_macros.count(name)) {
                 return true;
@@ -713,8 +735,7 @@ class Preprocessor {
         }
 
         if (cmd == "ifdef") {
-            std::string name;
-            iss >> name;
+            std::string name = next_token(body, pos);
             bool p = condActive(cond);
             bool v = macros.count(name);
             cond.push_back({ p, p && v, p && v });
@@ -722,8 +743,7 @@ class Preprocessor {
         }
 
         if (cmd == "ifndef") {
-            std::string name;
-            iss >> name;
+            std::string name = next_token(body, pos);
             bool p = condActive(cond);
             bool v = !macros.count(name);
             cond.push_back({ p, p && v, p && v });
@@ -731,7 +751,7 @@ class Preprocessor {
         }
 
         if (cmd == "if") {
-            std::string expr = trim_value(iss);
+            std::string expr = trim_from(body, pos);
             bool        p    = condActive(cond);
             bool        v    = false;
             if (p) {
@@ -744,7 +764,7 @@ class Preprocessor {
         }
 
         if (cmd == "elif") {
-            std::string expr = trim_value(iss);
+            std::string expr = trim_from(body, pos);
 
             if (cond.empty()) {
                 throw std::runtime_error("#elif without #if");
diff --git a/src/llama.rn/vendor/llama.cpp/src/llama-grammar.cpp b/src/llama.rn/vendor/llama.cpp/src/llama-grammar.cpp
index deffec8c..8d67bf20 100644
--- a/src/llama.rn/vendor/llama.cpp/src/llama-grammar.cpp
+++ b/src/llama.rn/vendor/llama.cpp/src/llama-grammar.cpp
@@ -96,7 +96,9 @@ static bool is_digit_char(char c) {
 }
 
 static bool is_word_char(char c) {
-    return ('a' <= c && c <= 'z') || ('A' <= c && c <= 'Z') || c == '-' || is_digit_char(c);
+    // '_' is accepted for the TTS grammars generated by rn-tts (e.g.
+    // SOPRANO_ID / SPEECH_ID rule names).
+    return ('a' <= c && c <= 'z') || ('A' <= c && c <= 'Z') || c == '-' || c == '_' || is_digit_char(c);
 }
 
 static std::pair<uint32_t, const char *> parse_hex(const char * src, int size) {
@@ -439,7 +441,19 @@ const char * llama_grammar_parser::parse_alternates(
         bool                is_nested) {
     llama_grammar_rule rule;
     const char * pos = parse_sequence(src, rule_name, rule, is_nested);
-    while (*pos == '|') {
+    while (true) {
+        // Allow alternates to continue on following lines (used by the
+        // rn-tts generated grammars): skip newlines + indentation iff the
+        // first non-space character is '|'. A line starting with anything
+        // else is the next rule, so leave pos untouched.
+        const char * next = pos;
+        while (*next == ' ' || *next == '\t' || *next == '\r' || *next == '\n') {
+            next++;
+        }
+        if (*next != '|') {
+            break;
+        }
+        pos = next;
         rule.push_back({LLAMA_GRETYPE_ALT, 0});
         pos = parse_space(pos + 1, true);
         pos = parse_sequence(pos, rule_name, rule, is_nested);
